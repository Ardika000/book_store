<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <springProperty name="ServiceName" source="spring.application.name"/>
    <springProperty name="ServiceProfile" source="spring.profiles.active"/>

    <property name="CONSOLE_LOG_PATTERN"
    value="%clr(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}){faint} %clr(-%5p){} %clr(${PID:-}){magenta} --- [%thread] %clr(%-40.40logger{39}){cyan} %clr(%4.4line:%-30.30M{29}){cyan} : [%X{traceId:-}-%X{spanId:-}] - %msg%n"/>

    <property name="FILE_LOG_PATTERN"
    value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}{faint} -%5p ${PID:-} --- [%thread] %-40.40logger{39} %4.4line:%-30.30M{29} : [%X{traceId:-}-%X{spanId:-}] - %msg%n"/>

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/bookstore-app.log</file>
        <encoder>
            <pattern>{FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/archived/bookstore-app-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <logger name="com.dproject.daniel_book_store" level="INFO" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="RollingFile" />
    </logger>

    <root level="INFO">
        <appender-ref ref="Console" />
        <appender-ref ref="RollingFile" />
    </root>

</configuration>